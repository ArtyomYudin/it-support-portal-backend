{
  name: "PACS TCP Socker Client",
  build: {
    dockerfile: "Dockerfile"
  },
  runArgs: ["--network=it_support"],
  //dockerComposeFile: "docker-compose.yaml",
  //service: "backend",
  workspaceFolder: "/opt/project/${localWorkspaceFolderBasename}",
  //forwardPorts: [8000,5432],
  //remoteUser: "root",
 // shutdownAction: "stopCompose",
  "customizations": {
    "jetbrains": {
      "settings": {
        "com.intellij:app:HttpConfigurable.use_proxy_pac": true
      },
      "backend" : "PyCharm"
    }
  },
}